import { randomId } from './index';

export const fndryFormConfig = `FndryFormProviderConfig${randomId()}`;
export const fndryFormState = `FndryFormProviderState${randomId()}`;
export const fndryFormParentForm = `FndryFormProviderParentForm${randomId()}`;
