//import {vsprintf} from 'sprintf-js'

export default function(defaultText, args){

    if (args === undefined) {
        args = [];
    }

    //let text = vsprintf(defaultText, args);

    return defaultText;
};